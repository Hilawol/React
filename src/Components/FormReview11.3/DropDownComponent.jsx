import React, { Component } from 'react'

export default class DropDownComponent extends Component {
  render() {
    return (
      <select name={this.props.dropeDownName}>

      </select>
    )
  }
}
